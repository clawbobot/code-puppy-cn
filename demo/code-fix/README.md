# Code Fix Demo

This deterministic project contains one defect in `calculator.py`.

Run:

```bash
python -m pytest
```

Expected initial result: one failing test.

Ask Code Puppy CN to activate the `code-fix` skill, repair the defect, and rerun
the tests. The task is complete when all tests pass without changing the tests.
